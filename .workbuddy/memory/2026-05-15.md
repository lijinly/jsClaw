# 2026-05-15

## jsClaw 配置文件重组

### 完成的工作
1. 删除空的 `workspaces/` 目录
2. 将 `members/` 移动到 `config/members/` 后又删除
3. 重构配置结构：Member 定义直接内嵌在 workspace JSON 中

### 最终结构
```
D:\jsClaw\config\
├── system.json          ← 系统配置（路径 + workspace 索引）
└── workspaces\
    └── default.json     ← workspace 定义（含 member 集合）
```

### Member 定义结构
```json
{
  "id": "coder",
  "name": "开发者",
  "identity": "...",      ← 身份信息
  "soul": "...",         ← 性格特征
  "skills": ["read", ...] ← 可用工具
}
```

### .gitignore
`config/` 整体排除 git

---

## Git 提交（13:57）

### commit `9975de8`
```
Complete framework docs + PascalCase refactoring
```

**变更统计**：31 files changed, 6448 insertions(+), 1679 deletions(-)

**主要变更**：
- 新增 9 个文档（docs/）
- 源代码重命名为 PascalCase
- 新增 DAG 任务系统（Goal.js, SubGoal.js, Task.js, Manager.js）
- 新增 Zone 生命周期管理
- 新增 jsClaw-dev-workflow skill
- memory 更新

**文件变更明细**：
```
A  docs/CONFIG.md
A  docs/GOAL_DAG_SYSTEM.md
A  docs/LLM.md
A  docs/MARKETPLACE.md
A  docs/MEMORY.md
A  docs/README.md
A  docs/SKILL_REGISTRY.md
A  docs/ZONE.md
M  docs/AGENT.md
M  docs/CONTEXT_MANAGER.md
M  docs/WORKSPACE.md
R  src/ContextManager.js -> src/Context.js
A  src/Goal.js
D  src/GoalTracker.js
A  src/Manager.js
A  src/SubGoal.js
A  src/Task.js
A  src/Zone.js
D  src/Team.js
D  src/globalWorkspace.js
M  src/agent.js
M  src/index.js
A  tests/TestGoalDag.js
M  tests/Test*.js
M  .workbuddy/memory/*.md
```

**已推送到 origin/main**

---

## Config.json 清理（14:09）

### 分析结果
- `src/Config.json` 是旧版 Team 系统遗留，未被代码引用
- 字段不一致（`teamMembers` vs `members`）
- `Config.js` 是当前配置架构

### Git 提交
```
commit d3c591c - Delete legacy Config.json (Team system)
1 file changed, 45 deletions(-)
```

**已推送到 origin/main**

## 代码审查 & Team.js 删除

### 完成的工作
1. 对 src 目录下所有核心文件进行代码审查
2. 完成完整审查报告（架构、错误处理、安全性）
3. 删除 `src/Team.js`（与 WorkSpace 职责重叠）

### 审查发现的主要问题
- Memory.js: ES Module 混用 require('fs')
- Agent.js: Prompt injection 防护缺失
- SkillRegistry.js: JSON.parse 缺少错误处理
- 建议按优先级逐步修复

### src 最终文件列表
Agent.js, Config.json, Context.js, Goal.js, Index.js, Llm.js, Marketplace.js, Member.js, Memory.js, Server.js, SkillRegistry.js, Config.js, WorkSpace.js, Zone.js

## 新增 Zone.js

### 功能
- Workspace 生命周期管理（创建、加载、卸载、保存、销毁）
- Zone 多 workspace 组织
- 状态持久化到 data/zones/

### 核心类
- `Zone`: 单个 Zone 管理器
- `ZoneManager`: 多 Zone 管理（单例模式）

### 目录结构
```
data/zones/<zoneId>/
├── meta.json
├── workspaces/<workspaceId>/
│   ├── state.json
│   └── .memory/
└── cache/
```

## 删除 GlobalWorkspace.js

### 变更
- Index.js 改用 Zone + WorkSpace 加载默认工作区
- GlobalWorkspace.js 已删除（功能由 Zone 替代）

### 新的初始化流程
1. Zone 初始化（zoneId='default'）
2. Workspace 创建/加载
3. WorkSpace 实例包装
